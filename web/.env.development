# During `vite dev` the UI is served from http://127.0.0.1:5173 but the daemon
# API/WS listens on its own loopback port, so override the same-origin default
# (see web/src/lib/config.ts) to point at the daemon.
VITE_API_BASE_URL=http://127.0.0.1:4517
