---
status: completed
title: Add MCP SDK dependency and test script
type: chore
complexity: low
dependencies: []
---

# Task 1: Add MCP SDK dependency and test script

## Overview
The workflow runner hosts an in-process HTTP MCP server, which requires the `@modelcontextprotocol/sdk` package, and runs unit tests with `bun test`. This task adds that dependency and a `test` script to `package.json` so later tasks can build and verify against a ready toolchain.

<critical>
- ALWAYS READ the PRD and TechSpec before starting
- REFERENCE TECHSPEC for implementation details — do not duplicate here
- FOCUS ON "WHAT" — describe what needs to be accomplished, not how
- MINIMIZE CODE — show code only to illustrate current structure or problem areas
- TESTS REQUIRED — every task MUST include tests in deliverables
</critical>

<requirements>
- The project MUST declare `@modelcontextprotocol/sdk` as a runtime dependency in `package.json`.
- `package.json` MUST define a `test` script that runs `bun test`.
- `bun install` MUST complete successfully and produce an updated `bun.lock`.
- The existing `dev`, `build`, and `typecheck` scripts MUST remain unchanged and functional.
- The installed SDK version MUST expose the Streamable HTTP server transport used by Task 3.
</requirements>

## Subtasks
- [x] 1.1 Add `@modelcontextprotocol/sdk` to the `dependencies` block of `package.json`.
- [x] 1.2 Add a `test` script that invokes `bun test`.
- [x] 1.3 Run `bun install` and commit the updated lockfile.
- [x] 1.4 Confirm `bun run typecheck` still passes with the new dependency present.

## Implementation Details
Modify `package.json` only. Add `@modelcontextprotocol/sdk` under `dependencies` (current deps: `@agentclientprotocol/sdk`, `@opentui/core`). Add `"test": "bun test"` under `scripts` alongside the existing `dev`, `build`, and `typecheck` entries. See TechSpec "Integration Points" and "Technical Dependencies" for why the SDK is required (it hosts the `handoff`/`finish` tools over the Streamable HTTP transport).

### Relevant Files
- `package.json` — declares dependencies and scripts; the file modified by this task.
- `bun.lock` — lockfile regenerated by `bun install`.
- `tsconfig.json` — strict NodeNext TypeScript config; the new dependency must resolve under it.

### Dependent Files
- `src/mcp.ts` — created in Task 3; imports `@modelcontextprotocol/sdk`.
- `src/workflow.test.ts` and other `*.test.ts` files — run via the new `test` script.

### Related ADRs
- [ADR-002: In-process HTTP MCP server for the handoff and finish tools](adrs/adr-002.md) — establishes the need for the MCP SDK dependency.

## Deliverables
- `package.json` with `@modelcontextprotocol/sdk` in `dependencies` and a `test` script.
- Updated `bun.lock`.
- Verification that `bun install` and `bun run typecheck` succeed **(REQUIRED)**.

## Tests
- Unit tests:
  - [ ] `bun install` exits 0 and `bun.lock` contains `@modelcontextprotocol/sdk`.
  - [ ] `bun run typecheck` exits 0 with the new dependency installed.
  - [ ] `bun test` runs and exits 0 (no test files yet is an acceptable pass).
- Integration tests:
  - [ ] A throwaway script that imports the Streamable HTTP server transport from `@modelcontextprotocol/sdk` resolves and runs without a module-resolution error.
- Test coverage target: >=80% (not applicable to dependency metadata; verification commands above stand in)
- All tests must pass

## Success Criteria
- All tests passing
- Test coverage >=80% (satisfied by the verification commands for this configuration-only task)
- `package.json` declares the MCP SDK dependency and a `test` script
- `bun install` and `bun run typecheck` both exit 0
