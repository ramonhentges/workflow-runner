# Task Memory: task_02.md

Keep only task-local execution context here. Do not duplicate facts that are obvious from the repository, task file, PRD documents, or git history.

## Objective Snapshot

- DONE & verified: `AppShell` re-expressed on shadcn `sidebar-07` inset pattern. 323/323 tests green; coverage Stmts 94 / Branches 82.97 / Funcs 96.19 / Lines 95.

## Important Decisions

- `AppShell` shell: `SidebarProvider` (carries `app-shell` testid) > `Sidebar collapsible="icon" variant="inset"` (carries `app-sidebar` testid) + `SidebarInset`. Nav = `SidebarMenuButton asChild` wrapping TanStack `Link`; active via `activeProps={{ 'data-active': 'true' }}` so the primitive's `data-[active=true]` styling fires and tests can assert it. Header in `SidebarInset` holds `SidebarTrigger` + a `data-testid="header-actions"` slot reserved for Task 9 `ModeToggle`.
- `DaemonStatus` copied verbatim into `SidebarHeader`; `CwdSwitcher` mounted unchanged in `SidebarFooter` (ADR-001: no internal refactor).

## Learnings

- Sidebar tooltips (Radix Tooltip) need `ResizeObserver` in jsdom — without the shim, any nav-link click in the suite throws "ResizeObserver is not defined" (broke the existing routing.test socket-lifecycle test). Fix lives in `test/setup.ts`.
- `useIsMobile` needs `window.matchMedia`; default the shim to `matches:false` (desktop branch) so the sidebar renders the desktop DOM the existing tests expect.

## Files / Surfaces

- `web/src/app/AppShell.tsx` (rebuilt), `web/src/app/AppShell.test.tsx` (new, 10 tests).
- `web/src/index.css` (sidebar tokens → oklch), `web/test/setup.ts` (matchMedia + ResizeObserver shims), `web/vite.config.ts` (coverage exclude).
- Generated by CLI: `components/ui/{sidebar,sheet,tooltip,separator,skeleton}.tsx`, `hooks/use-mobile.ts`; `radix-ui` dep added.

## Errors / Corrections

- `bunx shadcn add sidebar --yes` hung on the interactive "overwrite button.tsx?" prompt and wrote nothing. Re-ran as `yes N | bunx shadcn@latest add sidebar` to keep existing hand-authored button/input.

## Ready for Next Run

- Header `header-actions` slot is the mount point for Task 9 `ModeToggle`.
- `SidebarInset`/`SidebarTrigger` already in place; future tasks render inside the inset's `<main>` Outlet region.
